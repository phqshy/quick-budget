<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.fishy.testapp"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".app.ui.activity.CreateAccountActivity"
            android:exported="false" />
        <activity
            android:name=".app.ui.activity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".app.ui.activity.LoginActivity"
            android:exported="true"
            android:theme="@style/SplashScreenContent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".common.engines.ScheduledManager" android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="me.fishy.testapp.NOTIFICATION_ALARM"/>
            </intent-filter>
        </receiver>
        <service android:name=".common.engines.NotificationIntentManager" android:enabled="true"
            tools:ignore="Instantiatable">
        </service>
    </application>

</manifest>