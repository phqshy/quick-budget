package me.fishy.testapp.app.views;

import android.content.Context;
import android.util.AttributeSet;

public class BBVScheduled extends BoxBackgroundView{
    public BBVScheduled(Context context) {
        super(context);
    }

    public BBVScheduled(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public BBVScheduled(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
